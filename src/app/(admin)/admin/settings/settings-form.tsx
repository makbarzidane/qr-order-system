'use client'
import { useState } from 'react';import { useRouter } from 'next/navigation';import { updateSettings } from '../actions';import { Card,PageHeader,inputClass,primaryButton } from '@/components/admin/UI'
type Settings={name:string;address:string;whatsapp:string;invoiceNote:string;isOpen:boolean}
export function SettingsForm({settings}:{settings:Settings}){const r=useRouter();const[form,setForm]=useState(settings);const[msg,setMsg]=useState('');return <div><PageHeader title="Pengaturan cafe" description="Informasi ini dipakai pada storefront dan invoice."/>{msg?<p className="mb-4 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">{msg}</p>:null}<Card className="max-w-2xl p-6"><div className="grid gap-5"><label className="text-sm font-semibold">Nama cafe<input className={`${inputClass} mt-2`} value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label className="text-sm font-semibold">Alamat<textarea className={`${inputClass} mt-2`} rows={3} value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/></label><label className="text-sm font-semibold">Nomor WhatsApp<input className={`${inputClass} mt-2`} value={form.whatsapp} onChange={e=>setForm({...form,whatsapp:e.target.value})}/></label><label className="text-sm font-semibold">Catatan invoice<textarea className={`${inputClass} mt-2`} rows={3} value={form.invoiceNote} onChange={e=>setForm({...form,invoiceNote:e.target.value})}/></label><label className="flex items-center justify-between rounded-xl border border-slate-200 p-4"><span><span className="block font-semibold">Sistem menerima order</span><span className="text-sm text-slate-500">Matikan saat cafe tutup.</span></span><input type="checkbox" checked={form.isOpen} onChange={e=>setForm({...form,isOpen:e.target.checked})} className="h-5 w-5 accent-amber-500"/></label><button className={primaryButton} onClick={async()=>{await updateSettings(form);setMsg('Pengaturan tersimpan.');r.refresh()}}>Simpan pengaturan</button></div></Card></div>}
